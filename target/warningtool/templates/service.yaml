apiVersion: v1
kind: Service
metadata:
  name: {{ include "warningtool.fullname" . }}
  labels:
    {{- include "warningtool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "warningtool.selectorLabels" . | nindent 4 }}
